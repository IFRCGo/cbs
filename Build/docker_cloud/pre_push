#!/bin/bash

echo Tagging $IMAGE_NAME as $DOCKER_REPO:$SOURCE_COMMIT
docker tag $IMAGE_NAME $DOCKER_REPO:$SOURCE_COMMIT

echo Pushing $DOCKER_REPO:$SOURCE_COMMIT
docker push $DOCKER_REPO:$SOURCE_COMMIT
